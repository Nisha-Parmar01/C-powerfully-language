#include<stdio.h>

main()
{
	int n.i;
	float sum= 0.0,avg;
	
	printf("Enter the size:");
	scanf("%d",&n);
	
	int a[n];
	printf(" Enter array element:\n ");
	for(i=0;i<n;i++)
	{
		printf("a[%d] = ",i);
		scanf("%d",$a[i]);
		sum += a[i];
	}
	printf("Arrat\y element :\n");
	for(i=o;i<n;i++)
	{
		printf("a[%d] = %d\n",i,a[i]);
	}
	avg =sum/n;
	printf("average of array: %.2f\n",avg);
}
