#include<stdio.h>

main()
{
	
		int a,b,c,d;
		
		printf("Enter the value A:");
		scanf("%d",&a);
		printf("Enter the value A:");
		scanf("%d",&b);
		printf("Enter the value A:");
		scanf("%d",&c);
		printf("Enter the value A:");
		scanf("%d",&d);
		
		
	if(a<b)
	{
	  printf("%d is the maximum number");
	  	
	}else if(b<c)
	{
	   	printf("%d is the maximum number");
	
	}else if(c<d)
	{
	   	printf("%d is the maximum number");
	
	}
	else 
	{
	   	printf("%d is not the maximum number");
	}			
				
				
}
