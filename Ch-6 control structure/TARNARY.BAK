#include <stdio.h>
#include <conio.h>

main()
{
    int x,y,z;
    clrscr();
	  printf("Enter the first number X: ");
	  scanf("%d",&x);
	  printf("Enter the second number Y:");
	  scanf("%d",&y);
	  printf("Enter the third number Z:");
	  scanf("%d",&z);
	  clrscr();
	  (x<y)
		?    (x<z)
			?printf("%d is minimum...",x)
			:printf("%d is minimum...",z)
		:    (y<z)
			?printf("%d is minimum...",y)
			:printf("%d is minimume...",z);
    getch();

}