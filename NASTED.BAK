#include<stdio.h>
#include<conio.h>

main()
{      int x,y,z;
       clrscr();
       printf("Enter the first number:");
       scanf("%d",&x);
       printf("Enter the second number:");
       scanf("%d",&y);
       printf("Enter the Third number:");
       scanf("%d",&z);
       if(x>y)
       {
	  if(x>z)
	  {
		  clrscr();
		  printf("%d is a minimum",x);
	  }
	  else
	  {       clrscr();
		  printf("%d is a minimum",y);

	  }
       }
       else

       {
	  if(y>z)
	  {
		  clrscr();
		  printf("%d is a minimum",y);
	  }
	  else
	  {
		   clrscr();
		   printf("%d is a minimum ",z);
	  }
       }



       getch();
}




